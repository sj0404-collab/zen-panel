name: OpenCode

# OpenCode's own web UI on a GitHub runner, published through a cloudflared
# quick tunnel so the panel can open it in the same overlay as CLI-агент.
#
# Why a separate workflow: the Zen agent is this fork's own loop. OpenCode is
# a different product with its own models, tools and web client. Users pick
# linux or windows the same way they do for the agent.

on:
  workflow_dispatch:
    inputs:
      os:
        description: "Runner OS"
        type: choice
        options: [linux, windows]
        default: linux
      prompt:
        description: "Optional first prompt, sent once the web UI is up"
        type: string
        default: ""
      branch:
        description: "Working branch (created if absent)"
        type: string
        default: "opencode-work"

permissions:
  contents: write

jobs:
  opencode-linux:
    if: inputs.os == 'linux'
    runs-on: ubuntu-latest
    timeout-minutes: 360

    steps:
      - uses: actions/checkout@v4
        with:
          path: fork
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Prepare the working branch
        working-directory: fork
        run: |
          git config user.email "opencode@zen-panel"
          git config user.name  "OpenCode"
          BRANCH="${{ inputs.branch }}"
          if git ls-remote --exit-code --heads origin "$BRANCH" >/dev/null 2>&1; then
            git branch -f "$BRANCH" HEAD
            git checkout -q "$BRANCH"
            git push -q --force-with-lease origin "$BRANCH" || git push -q -f origin "$BRANCH"
          else
            git checkout -q -b "$BRANCH" && git push -q -u origin "$BRANCH"
          fi
          echo "WORK_BRANCH=$BRANCH" >> "$GITHUB_ENV"

      - name: Install OpenCode
        run: |
          set -e
          export PATH="$HOME/.opencode/bin:$HOME/.local/bin:$PATH"
          if ! command -v opencode >/dev/null 2>&1; then
            curl -fsSL https://opencode.ai/install | bash || true
          fi
          if ! command -v opencode >/dev/null 2>&1; then
            npm install -g opencode-ai
          fi
          command -v opencode
          opencode --version || true

      - name: Start OpenCode web and open the tunnel
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          FIRST_PROMPT: ${{ inputs.prompt }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          SYMBIOSIS_KEY: ${{ secrets.SYMBIOSIS_KEY }}
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          set -e
          export PATH="$HOME/.opencode/bin:$HOME/.local/bin:$PATH"
          cd "$GITHUB_WORKSPACE/fork"

          # No basic-auth password: the panel opens this in an iframe, and a
          # credentialed URL is stripped by many WebViews. The trycloudflare
          # hostname is the secret, same as the desks.
          nohup opencode web --port 4096 --hostname 0.0.0.0 \
            > /tmp/opencode.log 2>&1 &
          echo $! > /tmp/opencode.pid

          up=0
          for i in $(seq 1 40); do
            if curl -sf -o /dev/null -m 4 "http://127.0.0.1:4096/" \
              || curl -sf -o /dev/null -m 4 "http://127.0.0.1:4096/global/health" \
              || curl -sf -o /dev/null -m 4 "http://127.0.0.1:4096/session"; then
              up=1; break
            fi
            sleep 2
          done
          if [ "$up" != 1 ]; then
            echo "::error::OpenCode web did not start."
            tail -50 /tmp/opencode.log
            exit 1
          fi
          echo "OpenCode is up on 4096"

          if [ -n "$FIRST_PROMPT" ]; then
            opencode run --attach http://127.0.0.1:4096 --auto "$FIRST_PROMPT" \
              > /tmp/opencode-first.log 2>&1 || \
              echo "::warning::first prompt failed; the web UI is still up"
          fi

          URL=$(bash "$GITHUB_WORKSPACE/fork/tools/open_tunnel.sh" 4096 /tmp/oc-tunnel.log)
          if [ -z "$URL" ]; then
            echo "::error::no tunnel came up."
            tail -25 /tmp/oc-tunnel.log
            exit 1
          fi

          {
            echo "## OpenCode"
            echo ""
            echo "**[Open OpenCode]($URL/)**"
            echo ""
            echo "| | |"
            echo "|---|---|"
            echo "| Address | \`$URL\` |"
            echo "| Branch | \`$WORK_BRANCH\` |"
            echo "| Runner | ubuntu-latest |"
            echo ""
            echo "The web UI is the official OpenCode client. Treat the link as a password."
          } >> "$GITHUB_STEP_SUMMARY"

          echo "=================================================="
          echo " OpenCode: $URL/"
          echo "=================================================="

          GH_TOKEN="${{ secrets.GITHUB_TOKEN }}" \
            bash "$GITHUB_WORKSPACE/fork/tools/publish_session.sh" \
              "slot=opencode" "kind=OpenCode" "os=linux" \
              "url=$URL" "agentUrl=$URL/" \
              "branch=$WORK_BRANCH"

      - name: Keep the session alive
        run: |
          echo "Running until cancelled, or for six hours."
          export PATH="$HOME/.opencode/bin:$HOME/.local/bin:$PATH"
          for i in $(seq 1 1400); do
            sleep 15
            if curl -sf -o /dev/null -m 8 "http://127.0.0.1:4096/"; then
              continue
            fi
            echo "::warning::OpenCode stopped answering; restarting it."
            nohup opencode web --port 4096 --hostname 0.0.0.0 \
              > /tmp/opencode2.log 2>&1 &
            sleep 8
          done

      - name: Clear the published session
        if: always()
        run: |
          GH_TOKEN="${{ secrets.GITHUB_TOKEN }}" \
            bash "$GITHUB_WORKSPACE/fork/tools/publish_session.sh" \
              "slot=opencode" "state=ended" || true

      - name: Final save
        if: always()
        run: |
          cd "$GITHUB_WORKSPACE/fork" || exit 0
          if [ -n "$(git status --porcelain)" ]; then
            git add -A
            git commit -q -m "opencode final save $(date -u '+%Y-%m-%d %H:%M')" || true
            git push -q origin HEAD || true
          fi

  opencode-windows:
    if: inputs.os == 'windows'
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      - uses: actions/checkout@v4
        with:
          path: fork
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Prepare the working branch
        working-directory: fork
        shell: pwsh
        run: |
          git config user.email "opencode@zen-panel"
          git config user.name  "OpenCode"
          $branch = "${{ inputs.branch }}-windows"
          git ls-remote --exit-code --heads origin $branch *> $null
          if ($LASTEXITCODE -eq 0) {
            git branch -f $branch HEAD
            git checkout -q $branch
            git push -q --force-with-lease origin $branch
            if ($LASTEXITCODE -ne 0) { git push -q -f origin $branch }
          } else {
            git checkout -q -b $branch
            git push -q -u origin $branch
          }
          "WORK_BRANCH=$branch" | Out-File -FilePath $env:GITHUB_ENV -Append

      - name: Install OpenCode
        shell: pwsh
        run: |
          npm install -g opencode-ai
          opencode --version

      - name: Start OpenCode web and open the tunnel
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          FIRST_PROMPT: ${{ inputs.prompt }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          SYMBIOSIS_KEY: ${{ secrets.SYMBIOSIS_KEY }}
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          Set-Location "$env:GITHUB_WORKSPACE\fork"

          # cmd /c start, not Start-Process: a process started with
          # Start-Process is killed when its step ends.
          cmd /c start "" /b opencode web --port 4096 --hostname 0.0.0.0 `
            "1>$env:TEMP\opencode.log" "2>$env:TEMP\opencode.err"

          $up = $false
          foreach ($i in 1..40) {
            Start-Sleep -Seconds 2
            try {
              Invoke-WebRequest -Uri "http://127.0.0.1:4096/" -TimeoutSec 4 -UseBasicParsing | Out-Null
              $up = $true; break
            } catch { }
          }
          if (-not $up) {
            Write-Host "::error::OpenCode web did not start."
            Get-Content "$env:TEMP\opencode.log", "$env:TEMP\opencode.err" -Tail 50 -ErrorAction SilentlyContinue
            exit 1
          }
          Write-Host "OpenCode is up on 4096"

          if ($env:FIRST_PROMPT) {
            cmd /c "opencode run --attach http://127.0.0.1:4096 --auto `"$env:FIRST_PROMPT`""
            if ($LASTEXITCODE -ne 0) {
              Write-Host "::warning::first prompt failed; the web UI is still up"
            }
          }

          $cf = "$env:TEMP\cloudflared.exe"
          curl.exe -sL --retry 3 -o $cf `
            "https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-windows-amd64.exe"
          if (-not (Test-Path $cf)) { throw "could not download cloudflared" }

          $log = "$env:TEMP\cf-oc.log"
          cmd /c start "" /b "$cf" tunnel --url http://localhost:4096 --no-autoupdate --loglevel info `
            "1>$log" "2>$env:TEMP\cf-oc.err"

          $url = $null
          foreach ($i in 1..40) {
            Start-Sleep -Seconds 2
            $text = (Get-Content $log, "$env:TEMP\cf-oc.err" -Raw -ErrorAction SilentlyContinue) -join "`n"
            if ($text -match "https://[a-z0-9-]+\.trycloudflare\.com") { $url = $Matches[0]; break }
          }
          if (-not $url) {
            Write-Host "::error::no tunnel came up."
            Get-Content $log, "$env:TEMP\cf-oc.err" -Tail 25 -ErrorAction SilentlyContinue
            exit 1
          }

          foreach ($i in 1..20) {
            if ((curl.exe -s -o NUL -w "%{http_code}" -m 15 "$url/") -match "^[234]") { break }
            Start-Sleep -Seconds 3
          }

          $summary = @(
            "## OpenCode (Windows)",
            "",
            "**[Open OpenCode]($url/)**",
            "",
            "| | |",
            "|---|---|",
            "| Address | ``$url`` |",
            "| Branch | ``$env:WORK_BRANCH`` |",
            "| Runner | windows-latest, 4 cores, 16 GB |",
            "",
            "The web UI is the official OpenCode client. Treat the link as a password."
          ) -join "`n"
          $summary | Out-File -FilePath $env:GITHUB_STEP_SUMMARY -Append
          Write-Host "=================================================="
          Write-Host " OpenCode: $url/"
          Write-Host "=================================================="

          $env:GH_TOKEN = "${{ secrets.GITHUB_TOKEN }}"
          bash "$env:GITHUB_WORKSPACE/fork/tools/publish_session.sh" `
            "slot=opencode" "kind=OpenCode" "os=windows" `
            "url=$url" "agentUrl=$url/" `
            "branch=$env:WORK_BRANCH"

      - name: Keep the session alive
        shell: pwsh
        run: |
          Write-Host "Running until cancelled, or for six hours."
          for ($i = 0; $i -lt 1400; $i++) {
            Start-Sleep -Seconds 15
            $code = (curl.exe -s -o NUL -w "%{http_code}" -m 10 "http://127.0.0.1:4096/")
            if ($code -match "^[234]") { continue }
            Write-Host "::warning::OpenCode stopped answering ($code); restarting it."
            cmd /c start "" /b opencode web --port 4096 --hostname 0.0.0.0 `
              "1>$env:TEMP\opencode2.log" "2>$env:TEMP\opencode2.err"
            Start-Sleep -Seconds 10
          }

      - name: Clear the published session
        if: always()
        shell: pwsh
        run: |
          $env:GH_TOKEN = "${{ secrets.GITHUB_TOKEN }}"
          bash "$env:GITHUB_WORKSPACE/fork/tools/publish_session.sh" "slot=opencode" "state=ended"

      - name: Final save
        if: always()
        shell: pwsh
        run: |
          Set-Location "$env:GITHUB_WORKSPACE\fork"
          if (git status --porcelain) {
            git add -A
            git commit -q -m "opencode final save $(Get-Date -Format 'yyyy-MM-dd HH:mm')"
            git push -q origin HEAD
          }
