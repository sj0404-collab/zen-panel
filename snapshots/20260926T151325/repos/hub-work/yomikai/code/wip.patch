diff --git a/AGENT_SESSION.md b/AGENT_SESSION.md
index 538800b68..2d168bf5c 100644
--- a/AGENT_SESSION.md
+++ b/AGENT_SESSION.md
@@ -368,10 +368,10 @@ OCR отдавал только текст страницы, поэтому аг
 
 - Дата: 2026-09-26
 - Репозиторий: `/home/runner/hub-work/yomikai/code`
-- Ветка: локально `master` (HEAD `1e4df70`), удалённый `origin/main` —
-  `c177aeee50a5ff377d1ba7fcfa3e57375d5624d9`. Ветки не совпадают: перед
-  push изменения надо перенести на `main`, diff не потерять.
-- Последний подтверждённый коммит: `1e4df70`
+- Ветка: `main`. Локальный клон был склеен в один коммит `1e4df70`, поэтому
+  изменения перенесены патчем поверх `origin/main` (`c177aeee5`) — файлы
+  совпали байт в байт, конфликтов не было.
+- Последний подтверждённый коммит: `c177aeee5`
 
 ## Контекст
 
